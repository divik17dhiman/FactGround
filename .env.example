# Local environment configuration for FactGround
# Phase 0 intentionally does not require secrets or API keys.
# Copy this file to .env when local app configuration is needed.
#
# Example:
# APP_ENV=development
